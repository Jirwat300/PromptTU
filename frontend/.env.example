# ---- POP TU frontend env ----
# Base URL of the Express backend that exposes /api/ranking/*.
# Leave blank when the frontend and backend share the same origin.
#
# Examples:
#   VITE_API_URL=https://prompt-tu-backend.vercel.app
#   VITE_API_URL=http://localhost:3000
VITE_API_URL=
