{
  "name": "tupt-monorepo",
  "private": true,
  "description": "Vercel root: npm workspaces install once; build frontend; API via api/[...slug].js",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "vercel-build": "npm run build -w frontend"
  }
}
